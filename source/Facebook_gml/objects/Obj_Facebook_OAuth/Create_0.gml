/// @description Obsolete OAuth demo

show_message_async(
    "This OAuth demo belonged to the legacy HTML5 Facebook wrapper. "
    + "GMFacebook now uses the native Android/iOS SDK callback API."
);

instance_destroy();
